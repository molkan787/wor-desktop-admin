const config = {

    // =====================================
    devMode: true,
    useLocalServer: true,
    // =====================================

    appVersion: 10007,
    appVersionName: '1.0.7',

    appNameShort: 'WOR Admin',

    // Dev config
    dev: {
        firstPage: 'customers',
    }
    // =====================================
}

Object.freeze(config);
